[allowlist]
description = "Allow GitHub Actions secret references and workflow metadata"
regexes = [
  '''\$\{\{\s*secrets\.GITHUB_TOKEN\s*\}\}''',
  '''\$\{\{\s*github\.token\s*\}\}''',
  '''GITHUB_TOKEN.*\$\{\{''',
]
paths = [
  '''\.github/workflows/''',
]
